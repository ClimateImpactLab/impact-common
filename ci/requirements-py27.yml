name: test_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=2.7
  - numpy
  - pandas
  - xarray
  - pytest
  - pytest-mock
  - coverage
  - pip
  - pip:
      - git+https://github.com/ClimateImpactLab/open-estimate.git
      - impactlab-tools
      - metacsv
      